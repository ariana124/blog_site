<%- include("partials/header") -%>

    <h1>Home</h1>

    <p><%= homeContent %></p>

    <% totalPosts.forEach(function(post) { %>
        <div>
            <h3><%= post.title %></h3>
            <p><%= post.content %></p>
        </div>
    <% }); %>

<%- include("partials/footer") -%>
